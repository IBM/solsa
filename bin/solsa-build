#!/bin/sh

cd $1
if [[ ! -f package.json ]]; then echo "Missing package.json in $1"; exit; fi
if [[ ! -d node_modules ]]; then npm install --prod; fi
shift
docker build -f node_modules/solsa/Dockerfile . $*
